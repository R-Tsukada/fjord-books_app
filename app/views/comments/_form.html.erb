<%= form_with(model: [commentable, Comment.new], locale: true) do |f| %>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_area :body, required: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
