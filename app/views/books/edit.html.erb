<h1><%= t('activerecord.attributes.book.editing book') %></h1>

<%= render 'form', book: @book %>

<%= link_to t('activerecord.attributes.book.show'), @book %> |
<%= link_to t('activerecord.attributes.book.back'), books_path %>
