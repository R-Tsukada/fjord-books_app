<h1>User List</h1>
<% @users.each do |user| %>
 <table>
   <tr>
     <td><%= user.email %></td>
     <td><%=link_to t('.show'), user_path(user), class: 'navbar-link' %> </td>
   </tr>
 </table>
<% end %>
<%= paginate @users %>
<%= link_to t('views.common.back'), books_path %>
